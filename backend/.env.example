# Ken Finance backend configuration

PORT=5000

# Enables the LLM categorisation tier with Google Gemini. Without it the app still works —
# /api/categorize returns 503 and the user categorises manually, which is
# what merchant memory then learns from.
# Get your Google AI Studio API key at https://aistudio.google.com/
GEMINI_API_KEY=
