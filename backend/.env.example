# Ken Finance backend configuration

PORT=5000

# Enables the LLM categorisation tier. Without it the app still works —
# /api/categorize returns 503 and the user categorises manually, which is
# what merchant memory then learns from.
# Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=
